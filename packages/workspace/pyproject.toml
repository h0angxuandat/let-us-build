[project]
name = "lub-workspace"
version = "0.0.0"
description = "let-us-build — generated-code workspace + sandboxed build/test runner."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lub_workspace"]
